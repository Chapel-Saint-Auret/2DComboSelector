## Correlation Matrix Visualization

Displays the pairwise Kendall correlations between orthogonality metric values across all tested combinations (or the pairwise Spearman correlations between orthogonality metric ranks). 

It helps identify metrics with similar ranking behavior and supports their grouping before consensus rank calculation.

